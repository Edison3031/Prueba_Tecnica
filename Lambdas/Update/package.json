{
  "name": "update-user-lambda",
  "version": "1.0.0",
  "description": "Lambda function to update users in DynamoDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "make-dist": "mkdir dist",
    "copy-files": "copyfiles index.js controllers/**/*.js models/**/*.js dist && cd dist && npm install --omit=dev",
    "zip": "powershell Compress-Archive -Path ./dist/* -DestinationPath ./function.zip -Force",
    "build": "npm run clean && npm run make-dist && npm run copy-files && npm run zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.405.0",
    "@aws-sdk/lib-dynamodb": "^3.405.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "copyfiles": "^2.4.1"
  },
  "author": "",
  "license": "ISC"
}